"use client";

/**
 * Placeholder sections for project/workspace scope until tree registration is added.
 * App and viewport sections come from the settings registry (tree).
 */
import type { SettingsSection } from "./types";

export const PROJECT_SETTINGS_SECTIONS: SettingsSection[] = [];
export const WORKSPACE_SETTINGS_SECTIONS: SettingsSection[] = [];
