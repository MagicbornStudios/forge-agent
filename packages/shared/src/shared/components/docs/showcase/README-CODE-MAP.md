# Showcase code map

**`_showcase-code-map.json`** maps showcase demo IDs to source files. When a demo is mapped, its code tab shows the actual file content instead of hardcoded snippets.

**Add a mapping:** `"demo-id": "path/from/repo-root/to/file.tsx"`  
**Regenerate:** `pnpm docs:showcase:generate`
