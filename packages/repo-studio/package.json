{
  "name": "@forge/repo-studio",
  "version": "0.1.5",
  "description": "A new IDE centered around multiple coding agents, company agnostic but primarily integrated with Codex.",
  "author": "b2gdevs",
  "private": false,
  "type": "module",
  "main": "./src/desktop/main.cjs",
  "bin": {
    "forge-repo-studio": "./src/cli.mjs"
  },
  "files": [
    "src",
    "docs",
    "README.md"
  ],
  "scripts": {
    "build": "node -e \"console.log('repo-studio: no build step (source-distributed mjs)')\"",
    "test": "node --test \"src/__tests__/*.test.mjs\"",
    "desktop:dev": "node src/cli.mjs open --desktop-runtime --foreground",
    "desktop:build": "node src/desktop/build.mjs",
    "desktop:clean": "node src/desktop/clean.mjs --include-temp",
    "desktop:clean:all": "node src/desktop/clean.mjs --reset-dist --include-temp",
    "desktop:clean:scratch": "node src/desktop/clean.mjs --scratch-only",
    "desktop:smoke:launch": "node src/desktop/smoke-launch.mjs",
    "desktop:smoke:silent": "node src/desktop/smoke-install.mjs --kind silent --timeout-ms 420000 --launch",
    "desktop:verify:standalone": "node src/desktop/verify-standalone.mjs",
    "desktop:package:win": "node src/desktop/clean.mjs --reset-dist --include-temp && node src/desktop/build.mjs --require-standalone && node src/desktop/verify-standalone.mjs && electron-builder --config electron-builder.json --win dir && node src/desktop/clean.mjs --scratch-only && electron-builder --prepackaged dist/desktop/win-unpacked --config electron-builder.json --win portable nsis && node src/desktop/clean.mjs --scratch-only && electron-builder --prepackaged dist/desktop/win-unpacked --config electron-builder.silent.json --win nsis && node src/desktop/clean.mjs --include-temp",
    "publish": "npm publish"
  },
  "dependencies": {
    "@openai/codex": "^0.104.0",
    "chokidar": "^4.0.3",
    "yaml": "^2.8.1"
  },
  "optionalDependencies": {
    "keytar": "^7.9.0"
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12"
  }
}
