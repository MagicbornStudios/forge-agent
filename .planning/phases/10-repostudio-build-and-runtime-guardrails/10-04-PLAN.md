---
phase: 10-repostudio-build-and-runtime-guardrails
plan: 04
wave: 4
depends_on:
  - '10-03'
files_modified:
  - .github/workflows/ci.yml
  - AGENTS.md
  - docs/agent-artifacts/core/errors-and-attempts.md
  - .planning/phases/10-repostudio-build-and-runtime-guardrails/10-0*-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "RepoStudio guardrails must run in CI with minimal build/lint/test coverage."
    - "Agent docs must explicitly prevent CSS import dependency drift."
    - "Outputs must keep Forge Loop artifacts traceable and deterministic for repeat runs."
  artifacts:
    - path: ".planning/phases/10-repostudio-build-and-runtime-guardrails/10-04-SUMMARY.md"
      provides: "CI and documentation guardrail hardening summary."
      min_lines: 10
  key_links:
    - from: "AGENTS.md"
      to: "docs/agent-artifacts/core/errors-and-attempts.md"
      via: "agent recurrence prevention"
---

<objective>
Add minimal CI coverage and codify CSS dependency guard rules for RepoStudio changes.
</objective>

<context>
@AGENTS.md
@docs/agent-artifacts/core/errors-and-attempts.md
@package.json
</context>

<tasks>
<task type="auto">
  <name>Add minimal CI workflow for RepoStudio guardrails</name>
  <files>.github/workflows/ci.yml</files>
  <action>Create a workflow for install + RepoStudio build/lint + Forge Loop/Forge Env/RepoStudio package tests on PR/push.</action>
  <verify>Workflow validates core guardrail commands on healthy branch and fails on regressions.</verify>
  <done>Build/dependency regressions are blocked before merge by default CI checks.</done>
</task>

<task type="auto">
  <name>Codify CSS import/dependency rule for agents</name>
  <files>AGENTS.md, docs/agent-artifacts/core/errors-and-attempts.md</files>
  <action>Document app-local dependency requirement for CSS `@import` edits and required app build verification step.</action>
  <verify>Agent guidance references exact command expectations and recurrence prevention steps.</verify>
  <done>Future agents have explicit policy and examples to avoid repeating the same class of error.</done>
</task>

<task type="auto">
  <name>Publish Phase 10 summaries and close traceability</name>
  <files>.planning/phases/10-repostudio-build-and-runtime-guardrails/10-01-SUMMARY.md, .planning/phases/10-repostudio-build-and-runtime-guardrails/10-02-SUMMARY.md, .planning/phases/10-repostudio-build-and-runtime-guardrails/10-03-SUMMARY.md, .planning/phases/10-repostudio-build-and-runtime-guardrails/10-04-SUMMARY.md, .planning/STATE.md, .planning/TASK-REGISTRY.md</files>
  <action>Record completed work, evidence, and verification output for each plan wave and close phase status.</action>
  <verify>`forge-loop progress --json` and markdown planning artifacts agree on phase completion.</verify>
  <done>Phase 10 can be strict-verified without ambiguity.</done>
</task>
</tasks>
