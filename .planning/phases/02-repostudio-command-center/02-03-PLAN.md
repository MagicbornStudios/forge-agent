---
phase: 02-repostudio-command-center
plan: 03
wave: 3
depends_on: ['02-02']
files_modified: []
autonomous: true
must_haves:
  truths:
    - "RepoStudio app uses shared Studio editor/app primitives with docked workspaces and right-sidebar settings."
    - "Settings/codegen parity is established so command policy/view defaults are generated and reload deterministically."
  artifacts:
    - path: ".planning/phases/02-repostudio-command-center/02-03-SUMMARY.md"
      provides: "Execution summary for Studio-grade app parity and settings/codegen integration."
      min_lines: 12
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/02-repostudio-command-center/02-03-SUMMARY.md"
      via: "phase-to-summary traceability"
---

<objective>
Deliver Studio-grade RepoStudio app parity (dock layouts, settings/codegen, assistant panel reuse).
</objective>

<context>
@apps/repo-studio/src
@packages/shared/src/shared/components/editor
@packages/shared/src/shared/components/app
@apps/studio/components/settings
@apps/studio/components/editors/dialogue
</context>

<tasks>
<task type="auto">
  <name>Implement docked RepoStudio workspace with shared editor primitives</name>
  <files>apps/repo-studio/src/components/*</files>
  <action>Use shared `@forge/shared` editor/app components (dock layout, panel tabs, right sidebar) to match Studio operating patterns.</action>
  <verify>Env/Commands/Forge Loop/Docs/Assistant are available as docked workspaces with dark theme defaults.</verify>
  <done>RepoStudio app has Studio-grade UX parity.</done>
</task>

<task type="auto">
  <name>Add settings registry + codegen hooks for RepoStudio policies</name>
  <files>apps/repo-studio/src/*, packages/repo-studio/src/lib/config.mjs, .repo-studio/config.json</files>
  <action>Port the settings section/field registration model and generate deterministic defaults/IDs for command and view policy.</action>
  <verify>Settings values are generated, persisted, and reloaded consistently.</verify>
  <done>Right sidebar becomes authoritative policy editor.</done>
</task>

<task type="auto">
  <name>Reuse shared assistant panel infrastructure in RepoStudio app</name>
  <files>apps/repo-studio/src/components/*, packages/shared/src/shared/components/assistant-ui/*</files>
  <action>Mount the same assistant runtime/tool UI stack used in Studio, avoiding parallel assistant implementations.</action>
  <verify>Assistant tab functions with route-mode settings and clear readiness/error messaging.</verify>
  <done>RepoStudio assistant is feature-aligned with existing assistant infrastructure.</done>
</task>
</tasks>
