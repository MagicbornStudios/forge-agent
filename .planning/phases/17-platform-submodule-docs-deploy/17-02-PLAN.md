# Plan 17-02: Docs site Vercel deploy

## Goal

Ensure the docs site ([apps/docs](../../../apps/docs), @forge/docs) is Vercel-ready and document the Vercel project setup for deploying docs from forge-agent. Deploy the docs app to Vercel from forge-agent and record the docs URL for the env matrix (platform will set NEXT_PUBLIC_DOCS_APP_URL to this URL).

## Scope

- Ensure [apps/docs](../../../apps/docs) builds successfully with `pnpm --filter @forge/docs build` (already used in release workflow). Confirm output dir and framework (Next.js) for Vercel.
- Document Vercel project setup: root = forge-agent repo; build command (e.g. `pnpm install --frozen-lockfile && pnpm --filter @forge/docs build`); output directory per Next.js (e.g. `apps/docs/.next` or root-relative); install command. If monorepo root is used, Vercel must support pnpm workspace and filter.
- Add any **vercel.json** or env vars needed for the docs build (e.g. at repo root or apps/docs).
- Deploy docs to Vercel from forge-agent (separate Vercel project or same org); record the production docs URL for 17-03 (platform env) and 17-04 (matrix).

## Out of scope

- Platform deploy (17-03).
- Deployment matrix doc (17-04).

## Tasks

| ID | Task | Status |
|----|------|--------|
| FRG-1704 | Docs site: Vercel-ready (build/root/framework); document Vercel project setup for docs from forge-agent | Pending |
| FRG-1705 | Deploy docs app to Vercel (from forge-agent); record docs URL for env matrix | Pending |

## Done criteria

- Docs app is deployable to Vercel from forge-agent; setup documented.
- Docs URL recorded for NEXT_PUBLIC_DOCS_APP_URL and deployment matrix.
