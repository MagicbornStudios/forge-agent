---
status: complete
phase: 06-workspace-contributions-and-ide-navigation-core
updated: 2026-02-16T19:56:58.521Z
---

## Tests

### 1. RepoStudio app runtime components and hooks must stop using hand-rolled fetch and route all calls through typed API service modules.
expected: RepoStudio app runtime components and hooks must stop using hand-rolled fetch and route all calls through typed API service modules.
result: skipped
notes: Skipped in non-interactive mode.

### 2. Workspace-contributed menubar contracts must align with DS-07 and DS-08 in repo_studio_analysis decision artifacts.
expected: Workspace-contributed menubar contracts must align with DS-07 and DS-08 in repo_studio_analysis decision artifacts.
result: skipped
notes: Skipped in non-interactive mode.

### 3. Navigator must be generic and reusable across workspaces, with Code workspace as first rollout consumer (IN-01).
expected: Navigator must be generic and reusable across workspaces, with Code workspace as first rollout consumer (IN-01).
result: skipped
notes: Skipped in non-interactive mode.

### 4. Feature workspaces must consume typed service layer instead of direct fetch.
expected: Feature workspaces must consume typed service layer instead of direct fetch.
result: skipped
notes: Skipped in non-interactive mode.

### 5. Search implementation must be server-side and support plain + regex query modes (IN-02).
expected: Search implementation must be server-side and support plain + regex query modes (IN-02).
result: skipped
notes: Skipped in non-interactive mode.

### 6. Default excludes must remain disabled; include/exclude filters are user-provided only (IN-04).
expected: Default excludes must remain disabled; include/exclude filters are user-provided only (IN-04).
result: skipped
notes: Skipped in non-interactive mode.

### 7. Navigator/open-file surfaces must include git status decorations for changed/new files.
expected: Navigator/open-file surfaces must include git status decorations for changed/new files.
result: skipped
notes: Skipped in non-interactive mode.

### 8. Direct fetch detection guard is warning-only in this phase and must not fail CI by default.
expected: Direct fetch detection guard is warning-only in this phase and must not fail CI by default.
result: skipped
notes: Skipped in non-interactive mode.


## Summary

- total: 8
- pass: 0
- issue: 0
- skipped: 8
