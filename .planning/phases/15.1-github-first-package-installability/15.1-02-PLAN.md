# Plan 15.1-02: GitHub installability diagnostics and smoke checks

## Goal

Add repeatable diagnostics so GitHub install regressions fail early instead of being rediscovered manually.

## Scope

- Add a repository doctor for package installability rules.
- Add at least one smoke path that exercises GitHub-style package metadata.
- Document how to interpret failures and which phase owns each class of fix.

## Tasks

| ID | Task | Status |
|---|---|---|
| FRG-15104 | Add GitHub installability doctor/smoke verification | Complete |

## Current verification commands

- `pnpm run doctor:github-installability`
- `pnpm run smoke:github-installability`

## Current smoke result

- Passing standalone package installs: `@forge/forge-codegen`, `@forge/forge-env`, `@forge/forge-loop`, `@forge/repo-studio`, `@forge/types`, `@forge/ui`
- Explicitly skipped for Phase 15.1-03: app surfaces and repo-coupled packages that still depend on in-repo `file:` links

## Done criteria

- A single command surfaces package install blockers.
- Manifest regressions are machine-detectable.
- The doctor output is specific enough to point to the broken package boundary.
