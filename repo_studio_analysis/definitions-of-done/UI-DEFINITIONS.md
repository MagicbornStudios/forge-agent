# UI Definitions of Done

Applies to Repo Studio workspaces (Planning, Env, Commands, Story, etc.).

## Criteria

- All editor surfaces use compact tokens (`--control-*`, `--panel-padding`)
- No ad-hoc `px-*`/`py-*` in editor UIs
- FeatureGate for locked UI; CAPABILITIES from `packages/shared/src/shared/entitlements`
