# Forge Env Parity Chart

PRD features vs current Env workspace state. Target: Env workspace does everything; no standalone portal.

| PRD Feature | Current Env Workspace | Gap | Phase |
|-------------|----------------------|-----|-------|
| Per-key editing | None (doctor only) | Target editors with inputs; Save/Refresh | A |
| Copy-paste | None | Paste from .env/clipboard into inputs | A |
| Vendor scope | Not in default discovery | Add vendor to globs/config | A |
| Scope selector (pkg/app/vendor) | Implicit in targets | Explicit filter in UI | A |
| Mode-scoped inputs | Mode select only | Clear local/dev/prod per target | A |
| Unified env surface | Doctor snapshot only | Per-target editors; all targets in workspace | A |
