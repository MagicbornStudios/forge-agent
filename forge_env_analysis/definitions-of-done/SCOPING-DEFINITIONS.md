# Forge Env Scoping Definitions of Done

- **Target scope**: Each target is tagged as package | app | vendor | root
- **Discovery**: Configurable workspace globs include apps, packages, vendor when applicable
- **Mode**: local, preview, production, headless—each mode has required keys per profile
- **Paste scope**: User can choose target and mode when pasting; parsed keys map to correct inputs
