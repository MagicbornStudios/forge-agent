# Forge Env UI Definitions of Done

- Key inputs are clearly labeled with target and mode (local/dev/prod)
- Scope filter (package | app | vendor | root) is visible and functional
- Copy-paste control parses KEY=VALUE and fills correct target/mode inputs
- Secret keys are masked; optional reveal
- Save/Refresh persist to correct .env* file per target
