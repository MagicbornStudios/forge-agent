storage: ./verdaccio/storage

uplinks:
  npmjs:
    url: https://registry.npmjs.org/

packages:
  '@forge/*':
    access: $all
    publish: $all
  '@twick/*':
    access: $all
    publish: $all
    proxy: npmjs
  '**':
    access: $all
    proxy: npmjs

logs:
  - { type: stdout, format: pretty, level: http }
