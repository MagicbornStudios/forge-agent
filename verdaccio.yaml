storage: ./verdaccio/storage

uplinks:
  npmjs:
    url: https://registry.npmjs.org/

packages:
  '@forge/*':
    access: $all
    publish: $all
  '**':
    access: $all
    proxy: npmjs

logs:
  - { type: stdout, format: pretty, level: http }
