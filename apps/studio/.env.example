# Payload
# Copy needed vars to .env or .env.local; this file is documentation only.
PAYLOAD_SECRET=dev-secret-change-me
# Optional. If unset, DB is apps/studio/data/payload.db (created automatically).
# DATABASE_URI=file:./data/payload.db

# Optional: seed users
# SEED_ADMIN_EMAIL=admin@forge.local
# SEED_ADMIN_PASSWORD=admin12345

# Marketing / customer-facing (optional)
# Resend: welcome email on newsletter subscribe (set RESEND_API_KEY to enable)
# RESEND_API_KEY=re_xxxx
# FROM_EMAIL=Forge <onboarding@resend.dev>

# Stripe: checkout and webhook (set STRIPE_SECRET_KEY + STRIPE_PRICE_ID_PRO + STRIPE_WEBHOOK_SECRET)
# STRIPE_SECRET_KEY=sk_test_xxxx
# STRIPE_PRICE_ID_PRO=price_xxxx
# STRIPE_WEBHOOK_SECRET=whsec_xxxx
# Platform Stripe account id (for reference / Stripe Dashboard parity). Connect API uses secret key.
# STRIPE_ACCOUNT_ID=acct_xxxx
# Platform requirements for Connect and product: support and legal URLs (update when legal/support in place)
# STRIPE_SUPPORT_EMAIL=you@example.com
# STRIPE_SUPPORT_URL=https://example.com/support
# PRIVACY_POLICY_URL=https://example.com/privacy-policy
# TERMS_OF_SERVICE_URL=https://example.com/terms-of-service

# ElevenLabs: character voice preview and selection
# ELEVENLABS_API_KEY=sk_xxxx

# Better Upload (direct-to-S3 media upload in Generate Media modal)
# Requires an S3-compatible bucket (AWS S3, Cloudflare R2, etc.).
# BETTER_UPLOAD_BUCKET=my-bucket
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=us-east-1

# App URL (for redirects; marketing may use NEXT_PUBLIC_STUDIO_APP_URL pointing here)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# CORS allowlist for cross-origin Platform -> Studio API requests with credentials.
# Use a comma-separated list for additional origins in non-local environments.
# CORS_ALLOWED_ORIGINS=http://localhost:3001
# Optional single-origin alias:
# PLATFORM_APP_URL=http://localhost:3001

# PostHog: analytics and feature flags (e.g. video-editor-enabled). Use different keys for dev vs prod if using separate projects.
# NEXT_PUBLIC_POSTHOG_KEY=phc_xxxx
# NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Logging (Studio): env-driven structured logger; file path is in .gitignore.
# LOG_LEVEL=debug|info|warn|error (default: info)
# LOG_FILE=.logs/studio.log
# LOG_NAMESPACES=model-router,copilotkit (optional; debug only for these namespaces)
# Dev-only: send browser logs to same file (set both to enable).
# ALLOW_CLIENT_LOG=1
# NEXT_PUBLIC_LOG_TO_SERVER=1
