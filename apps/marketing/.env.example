# Studio app URL (for API and "Open app" link). Use same origin or configure CORS/cookie domain for auth.
NEXT_PUBLIC_STUDIO_APP_URL=http://localhost:3000

# Optional: PostHog for analytics (page views + events). Use different keys for dev vs prod if using separate projects.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
